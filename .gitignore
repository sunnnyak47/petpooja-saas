node_modules/
.env
# Ignore every .env anywhere (backend/.env, frontend/.env, etc.) but keep examples
**/.env
**/.env.*
!**/.env.example
# Image assets in uploads only (not app icons/splash)
uploads/*.png
uploads/*.webp
uploads/*.jpg
dist/
logs/
uploads/

scratch/

# WhatsApp Web auth credentials (Baileys session files — never commit)
wa-auth/
backend/wa-auth/
.vercel

# Database backups — pg_dump output is full data, never commit
backups/
backend/backups/
*.dump
